# 《旗舰元年》协作与代理说明

## 必须先读

当前归档版本由根目录 `VERSION` 确定，目前为 6.2。开始任何设计、修改或重建前，先读 `versions/6.2/work/v62_data.json`、`versions/6.2/work/v62_rulebook.json`、`versions/6.2/work/v62_rules.md`、`versions/6.2/work/v62_planning.md` 与 `versions/6.2/outputs/旗舰元年_6.2_修改与校验说明.md`。不要从 6.1、6.0、5.x、早期版本或聊天摘要重造缺失卡牌。

## 授权与版本边界

讨论不等于修改授权。用户没有明确要求修改时，不改文件、不生成新版、不推送提交。提案与现行规则分开。归档上传本身不授权设计改动、公开仓库、开启网站或选择开源许可证。

`versions/6.2/`、`versions/6.0/` 与 `versions/5.0/` 都是原始已交付快照。后续修改在工作分支或新版本目录进行；不得把新规则写回已归档快照。6.2 不是可与旧版本卡面混用的补丁；旧版 `source/` 仅为历史追溯依赖。

## 已确认的 6.2 约束

1. 每位玩家每个本人回合分配 3 个工作点；共同发布仅由玩家主动预热推进，不设固定轮数。
2. 一套 257 张主牌和 8 张牌市；稳定、定制、研发、合同、设施和方案均从主牌流取得，所有五代从开局进入。
3. 稳定和定制免费取得；稳定入库时付款。任一现存产品引用都会锁定相应稳定库栏，全部引用结束后才可替换。
4. 一次组装只形成一批现货、占一个产品位；一批只能投一个零售市场、一张合同或留库。未售批次原样保留，成交后整场奖励结束才释放引用。
5. 不恢复芯片配额、公司产能、型号库存数量条、自动回款、固定上市印刷收入、电子发牌、自动结算或对局保存。
6. 参考机在全员锁价和投放后，按预定世代随机抽取并保留。挑战/科技奖励遵守实际成交与已部署完成研发的条件。
7. 收入 60 或科技 30 触发最后阶段，全员各再一回合后最终发布，比较收入与科技 ×2 的较高者。
8. 6.2 仍属结构简化测试版。静态检查、排版检查或重建记录不能声称为充分平衡、真人实测时长或打印机套准。

## 数据、生成与检查

JSON、规则正文和图鉴应保持一致。发现文件冲突应明确报告，不以推测悄悄修正。同步检查卡牌、版图、实体组件、主牌/参考机双面打印件、组件份数、时序和引用编号。修改 `v62_rulebook.json` 后先运行同步脚本再导出。

`versions/6.2/work/migrate_v62.py` 与 `write_rulebook.py` 是初次迁移/写作脚本，会覆盖手改内容；不要把它们当普通构建命令。读取既有校验记录与本次实际测试要区分。不得上传字体文件、凭据、个人目录备份或无关聊天。

## 远程操作

未经明确授权不修改可见性或许可。不重写远端历史、不强推、不删除现有仓库。推送前列出实际变更，推送后核对远端提交、标签和文件。未成功执行时不得报告已创建或已上传。
