# 《旗舰元年》协作与代理说明

## 必须先读

当前归档版本由根目录 `VERSION` 确定，目前为 6.0。开始任何设计、修改或重建前，先读 `versions/6.0/work/v60_data.json`、`versions/6.0/work/v60_rules.md`、`versions/6.0/outputs/旗舰元年_6.0_修改与校验说明.md` 与 `versions/6.0/work/v60_planning.md`。不要从 5.x、早期版本或聊天摘要重造缺失卡牌。

## 授权与版本边界

讨论不等于修改授权。用户没有明确要求修改时，不改文件、不生成新版、不推送提交。提案与现行规则分开。归档上传本身不授权设计改动、公开仓库、开启网站或选择开源许可证。

`versions/6.0/` 与 `versions/5.0/` 都是原始已交付快照。后续修改在工作分支或新版本目录进行；不得把新规则写回已归档快照。6.0 不是可与 5.x 卡面混用的补丁；5.x 的 `source/` 仅为历史追溯依赖。

## 已确认的 6.0 约束

1. 每位玩家每个本人回合分配 3 个工作点；共同发布仅由玩家主动预热推进，不设固定轮数。
2. 2/3/4 人共同发布终点为 12/16/20；收入 60 或科技 30 触发最后阶段，全员各再一回合后最终发布。
3. 最终成绩比较收入与科技 ×2 的较高者，不恢复 5.0 的双轨相遇或固定科技换算。
4. 四市场固定为大众消费、电竞娱乐、摄影创作、商务办公；参考机每代公开随机 1 款，挑战奖励要求严格胜出与真实零售成交。
5. 型号最多 4 款；部件与部署持续占用至退市。制造先付款、成交后取得收入；芯片配额不因退市或改款重置。
6. 不引入无债融资、自动回款、固定上市印刷收入、必需 P 企划、电子发牌、自动结算或对局保存。
7. 6.0 仍属首测版。静态情境、文件检查或重建记录不能声称为充分平衡、真人实测时长或打印机套准。

## 数据、生成与检查

JSON、规则正文和图鉴应保持一致。发现文件冲突应明确报告，不以推测悄悄修正。同步检查卡牌、版图、实体组件、组件份数、时序和引用编号。`versions/6.0/work/make_data.py` 是初次迁移/写作脚本，会覆盖手改数据；不要把它当作普通构建命令。

读取既有校验记录与本次实际测试要区分。不得上传字体文件、凭据、个人目录备份或无关聊天。

## 远程操作

默认私有，不擅自修改可见性或许可。不重写远端历史、不强推、不删除现有仓库。推送前列出实际变更，推送后核对远端提交、标签和文件。未成功执行时不得报告已创建或已上传。
